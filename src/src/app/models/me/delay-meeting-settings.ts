export class DelayMeetingSettings {
    public requestId: any;
    public timeDelay: string;
    constructor(requestId: any, timeDelay: string) {
        this.requestId = requestId;
        this.timeDelay = timeDelay;
    }
}
